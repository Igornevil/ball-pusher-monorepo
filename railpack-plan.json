{
  "node": "22.19.0",
  "packageManager": "pnpm",
  "install": "pnpm store prune && pnpm install --network-concurrency 1",
  "build": "pnpm build",
  "start": "npx vite preview --port $PORT --host",
  "cache": {
    "paths": ["node_modules", "packages/**/node_modules", ".pnpm-store"]
  }
}
