{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run build && echo 'Frontend dist contents:' && ls -la /app/packages/frontend/dist/"
  },
  "deploy": {
    "startCommand": "cd packages/backend && node dist/index.js"
  }
}
