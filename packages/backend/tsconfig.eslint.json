{
  "extends": "./tsconfig.json", // Наследуем все основные настройки
  "include": [
    "src/**/*.ts", // Указываем, что ESLint должен анализировать все .ts файлы в src
    "../eslint.config.mjs" // Также включаем сам конфиг ESLint
  ]
}
